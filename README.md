## Cypress task for coding challenge
### Prerequisites:
Homebrew (MacOS), Node.js, 
### How to run: 
`npm install`  
`npm cypress open`- for running tests in Cypress runner  
`npx cypress run` - for running tests in command line 